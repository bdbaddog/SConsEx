print("************************************* [Building SConsEx/packages]")

import os
Import('env')

# Ordered build ...
Scripts = [
   'core/SConstruct',
   'numerics/SConstruct'
]

for script in Scripts:
   SConscript( script )

