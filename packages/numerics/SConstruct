print("************************************* [Building SConsEx/packages/numerics]")

import os

# Setup environment
# Settings
include = '../../packages'
env = Environment(CPPPATH=include)

# Ordered build ...

SourceFiles = [
   'constants.cpp',
   'linear_funcs.cpp'
]

SharedLibrary('../../lib/SConsEx_numerics',SourceFiles)

